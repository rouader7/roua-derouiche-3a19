<!DOCTYPE html>
<html>
<head>
    <title>Authors List</title>
</head>
<body>
    <h1>List of Authors</h1>
    <ul>
        {% for author in authors %}
            <p>
                {{author.username}}
                {{author.email}}

            </p>
        {% endfor %}

        {{ nom }}
    </ul>
</body>
</html>
